services:
  ocr-pymupdf:
    build: .
    container_name: ocr-pymupdf
    volumes:
      - ./pdfs:/app/pdfs
      - ./result:/app/result
      - ./src:/app/src
      - ./data:/app/data
    stdin_open: true
    tty: true
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app/src